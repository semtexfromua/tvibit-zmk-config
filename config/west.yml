manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned to a stable release (main dropped the nice_nano_v2 board name).
      # Bump to a newer tag from https://github.com/zmkfirmware/zmk/releases when ready.
      revision: v0.3.0
      import: app/west.yml
  self:
    path: config
