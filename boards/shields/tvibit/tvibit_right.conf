# Per-half overrides for the RIGHT (peripheral) half.
# The OLED is enabled on both halves via config/tvibit.conf; if you only have a
# display on one side, disable it on the other here, e.g.:
#   CONFIG_ZMK_DISPLAY=n
